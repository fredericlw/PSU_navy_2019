/*
** EPITECH PROJECT, 2019
** my_arraylen.c
** File description:
** A function that counts and returns the number of lines found in an array
*/

#include "../../include/my.h"

int my_arraylen(char **array)
{
    int i = 0;

    for (i = 0; *array != '\0'; i++)
    return (i);
}